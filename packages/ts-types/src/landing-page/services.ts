export interface ServiceProps {
    title: string;
    description: string;
  }
    
  export interface ServiceSectionProps {
    services: ServiceProps[];
    heading: string;
    description: string;
  }